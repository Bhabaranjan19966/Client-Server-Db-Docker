version: '3'
services:
  app:
    container_name: backend110
    build: ./server/.
    env_file:
      - ./variables.env
    links:
      - mongo
    ports:
      - 5002:5002  
    depends_on:
      - mongo
  
  
  mongo:
    container_name: database119
    image: mongo
    ports:
      - 27020:27017

